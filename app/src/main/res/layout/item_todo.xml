<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="?attr/selectableItemBackground"
    android:baselineAligned="false"
    android:minHeight="64dp"
    android:orientation="horizontal">

    <!-- Priority is shown as a colour stripe so it reads at a glance. -->
    <View
        android:id="@+id/priority_stripe"
        android:layout_width="6dp"
        android:layout_height="match_parent"
        tools:background="@color/priority_high" />

    <CheckBox
        android:id="@+id/todo_check"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center_vertical"
        android:contentDescription="@string/action_toggle_complete"
        android:minWidth="48dp"
        android:minHeight="48dp" />

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_gravity="center_vertical"
        android:layout_weight="1"
        android:orientation="vertical"
        android:paddingEnd="16dp"
        android:paddingBottom="12dp"
        android:paddingTop="12dp"
        tools:ignore="RtlSymmetry">

        <TextView
            android:id="@+id/todo_title"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:ellipsize="end"
            android:maxLines="2"
            android:textAppearance="?attr/textAppearanceSubtitle1"
            tools:text="牛乳を買う" />

        <TextView
            android:id="@+id/todo_description"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:ellipsize="end"
            android:maxLines="2"
            android:textAppearance="?attr/textAppearanceBody2"
            android:textColor="@color/meta_text"
            tools:text="低脂肪のもの" />

        <TextView
            android:id="@+id/todo_meta"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:ellipsize="end"
            android:maxLines="2"
            android:textAppearance="?attr/textAppearanceCaption"
            android:textColor="@color/meta_text"
            tools:text="買い物 · 期限 7/25 · 毎週 (月・水)" />
    </LinearLayout>
</LinearLayout>
